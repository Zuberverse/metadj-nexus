# MetaDJ Nexus — Environment Variables (Example)
#
# Copy this file to .env.local for local development:
#   cp .env.example .env.local
#
# Notes:
# - Never commit real secrets.
# - Boolean flags use "true"/"false" (some also accept 1/0).
#

# -----------------------------------------------------------------------------
# Core
# -----------------------------------------------------------------------------
NODE_ENV=development
PORT=8100

# -----------------------------------------------------------------------------
# Public URLs (used for metadata / OG tags)
# -----------------------------------------------------------------------------
NEXT_PUBLIC_APP_URL=https://metadj.ai
# NEXT_PUBLIC_PREVIEW_URL=https://your-preview-url.example
# NEXT_PUBLIC_APP_VERSION=0.9.46

# -----------------------------------------------------------------------------
# MetaDJai — Models & Providers (Vercel AI SDK)
# -----------------------------------------------------------------------------
# Server default provider (openai | google | anthropic | xai)
# AI_PROVIDER=openai

# OpenAI (GPT) — Priority 1 (default)
# PRIMARY_AI_MODEL=gpt-5.2-chat-latest
# OPENAI_API_KEY=sk-...

# Google (Gemini) — Priority 2
# GOOGLE_AI_MODEL=gemini-3-flash-preview
# GOOGLE_API_KEY=AIza...

# Anthropic (Claude) — Priority 3
# ANTHROPIC_AI_MODEL=claude-haiku-4-5
# ANTHROPIC_API_KEY=sk-ant-...

# xAI (Grok) — Priority 4
# XAI_AI_MODEL=grok-4-1-fast-non-reasoning
# XAI_API_KEY=xai-...

# Optional speech-to-text model for Journal / voice input
# OPENAI_TRANSCRIBE_MODEL=gpt-4o-mini-transcribe-2025-12-15

# Optional infrastructure flags
# AI_FAILOVER_ENABLED=true
# AI_CACHE_ENABLED=true

# AI timeout configuration (milliseconds)
# AI_TIMEOUT_STREAM=60000
# AI_TIMEOUT_CHAT=30000
# AI_TIMEOUT_TRANSCRIBE=60000
# AI_TIMEOUT_TOOLS=30000

# AI response caching
# AI_CACHE_TTL_MS=300000
# AI_CACHE_MAX_SIZE=100

# General AI request timeout (fallback)
# AI_REQUEST_TIMEOUT_MS=30000

# -----------------------------------------------------------------------------
# Media Storage — Replit App Storage
# -----------------------------------------------------------------------------
# Production buckets (set via Replit Secrets / hosting environment)
# MUSIC_BUCKET_ID=replit-objstore-...
# VISUALS_BUCKET_ID=replit-objstore-...

# Legacy alias for MUSIC_BUCKET_ID (still supported)
# AUDIO_BUCKET_ID=replit-objstore-...

# Development-only fallback buckets (used when MUSIC_BUCKET_ID/VISUALS_BUCKET_ID are unset)
# DEV_FALLBACK_MUSIC_BUCKET_ID=replit-objstore-...
# DEV_FALLBACK_VISUALS_BUCKET_ID=replit-objstore-...

# SECURITY: Allow fallback buckets in production (not recommended)
# ALLOW_OBJECT_STORAGE_FALLBACK=false

# -----------------------------------------------------------------------------
# Rate Limiting (optional) — Upstash Redis
# -----------------------------------------------------------------------------
# UPSTASH_REDIS_REST_URL=https://...
# UPSTASH_REDIS_REST_TOKEN=...

# -----------------------------------------------------------------------------
# Analytics (optional) — Plausible
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_PLAUSIBLE_DOMAIN=metadj.ai
# NEXT_PUBLIC_PLAUSIBLE_API_HOST=https://plausible.io

# -----------------------------------------------------------------------------
# Logging (optional) — Webhook forwarding
# -----------------------------------------------------------------------------
# LOGGING_WEBHOOK_URL=https://...
# LOGGING_SHARED_SECRET=... (>=32 chars)
# LOGGING_CLIENT_KEY=... (>=32 chars; must match NEXT_PUBLIC_LOGGING_CLIENT_KEY)
# NEXT_PUBLIC_LOGGING_CLIENT_KEY=... (>=32 chars; must match LOGGING_CLIENT_KEY)

# -----------------------------------------------------------------------------
# Dream / Daydream (optional)
# -----------------------------------------------------------------------------
# DAYDREAM_API_KEY=...
# DAYDREAM_API_GATEWAY=https://api.daydream.live
# DAYDREAM_WHIP_ALLOWED_HOSTS=daydream.live,sdaydream.live,livepeer.com,livepeer.studio,livepeer.cloud,lp-playback.studio,localhost,127.0.0.1
# DAYDREAM_WHIP_ALLOW_DEV=true

# -----------------------------------------------------------------------------
# Dev-only endpoints
# -----------------------------------------------------------------------------
# DEV_SECRET=... (required to enable /api/dev/clear-rate-limits)
# DEV_API_TOKEN=... (legacy; optional additional check)

# -----------------------------------------------------------------------------
# Platform-provided environment variables (usually set automatically)
# -----------------------------------------------------------------------------
# REPLIT_ENV=
# REPLIT_DEPLOYMENT=
# REPLIT_APP_URL=
# REPL_ID=
# VERCEL_ENV=
